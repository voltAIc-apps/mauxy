# Template only -- DO NOT commit real values.
# Create the secret with:
#   kubectl create secret generic mautic-unsubscribe-credentials \
#     --from-literal=MAUTIC_BASE_URL=https://engage.wapsol.de \
#     --from-literal=MAUTIC_USERNAME=<user> \
#     --from-literal=MAUTIC_PASSWORD=<pass> \
#     --from-literal=ADMIN_API_KEY=<key> \
#     -n simplify-web
apiVersion: v1
kind: Secret
metadata:
  name: mautic-unsubscribe-credentials
  namespace: simplify-web
type: Opaque
stringData:
  MAUTIC_BASE_URL: "https://engage.wapsol.de"
  MAUTIC_USERNAME: ""
  MAUTIC_PASSWORD: ""
  ADMIN_API_KEY: ""
